# Open With IINA

Subscribes to `ui.openWith` and contributes an IINA entry to the Open With menu
for video items when the requesting client is running macOS. IINA is macOS-only,
so the entry is hidden on every other platform.

URL scheme: `iina://weblink?url=<stream-url>`.
