// This API key is intentionally committed to the public Aviato official-plugins
// repository (https://github.com/aviato-media/official-plugins) so that the
// open-source TMDb plugin works out of the box for end users of the Aviato
// media server.
//
// TMDb's terms permit embedding a key in a client application. Users who want
// to use their own key (e.g. to track quota separately or to avoid rate
// limits shared with the default key) can override this value via the plugin
// settings — see `tmdbApiKey` in AVIATO_PLUGIN_CONFIG.
export const DEFAULT_TMDB_API_KEY = 'f35f01368f484cda58577ea1a8375e28'
