{
  "name": "@aviato-media/audible",
  "version": "1.0.0",
  "type": "module",
  "description": "Indexes audiobook files by resolving metadata from Audible (via the audnex.us API).",
  "license": "MIT",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --minify --external zod",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@aviato-media/plugin-sdk": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
