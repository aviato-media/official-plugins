{
  "name": "@aviato-media/external-subtitles",
  "version": "1.0.1",
  "type": "module",
  "description": "Discovers external subtitle sidecar files (.srt/.ass/.ssa/.vtt/.sub) and surfaces embedded subtitle streams already extracted by ffprobe.",
  "license": "MIT",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --minify --external zod",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@aviato-media/plugin-sdk": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
