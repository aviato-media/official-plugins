{
  "name": "@aviato-media/external-metadata",
  "version": "1.0.1",
  "type": "module",
  "description": "Reads sidecar metadata files (.nfo for video, .opf for ebooks) and merges their fields, IDs, artwork, and entities into the bundle.",
  "license": "MIT",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --minify --external zod",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@aviato-media/plugin-sdk": "catalog:",
    "fast-xml-parser": "^5.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
