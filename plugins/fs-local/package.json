{
  "name": "@aviato-media/fs-local",
  "version": "0.1.0",
  "type": "module",
  "description": "Add local files to your libraries.",
  "license": "MIT",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --minify --external zod",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "dependencies": {
    "@aviato-media/plugin-sdk": "^1.0.0",
    "mime-types": "^3.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/mime-types": "^2.1.0",
    "typescript": "^5.9.3"
  }
}
